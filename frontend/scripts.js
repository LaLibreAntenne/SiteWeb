// Information du serveur

// Import des constantes d'informations
const memberCount = document.getElementById('member-counts');
const staffCount = document.getElementById('staff-counts');
const openDate = document.getElementById('open-date');

// Définition des constantes
const manuelMemberCount = 25;
const manuelStaffCount = 3;
const manuelOpenDate = "1/08/2026";

// Application des constantes
memberCount.innerText = manuelMemberCount;
staffCount.innerText = manuelStaffCount;
openDate.innerText = manuelOpenDate;